export function shuffle(a: number[]) {
  let j = 0;
  let x = 0;
  let i = 0;
  for (i = a.length - 1; i > 0; i--) {
    j = Math.floor(Math.random() * (i + 1));
    x = a[i];
    a[i] = a[j];
    a[j] = x;
  }
  return a;
}
